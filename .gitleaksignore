# Fałszywy alarm nocnego pełnego skanu historii (schedule, fetch-depth: 0).
# Linia to CELOWY rekwizyt testu PII-safety — test dowodzi, że treść studenta
# NIE wycieka do logów walidacji. Żaden realny sekret. Bieżąca wersja pliku ma
# dodatkowo inline `gitleaks:allow`; ten wpis wycisza HISTORYCZNY commit.
31ba61ac11b444d93ee17134e4f8c3b30867e360:src/lib/__tests__/log.test.ts:generic-api-key:68

# 1E.7 L3 — ten sam wzorzec. Stała PATH_KEY w teście placementu niesie SLUG
# ścieżki curriculum (rekwizyt testu integracyjnego), nie sekret; regułę
# generic-api-key odpala nazwa stałej zawierająca KEY + długość napisu.
# ⚠ NIE cytuj tu wartości — komentarz z literałem sam odpala regułę (na tym
# pliku), co kosztowało jeden przebieg CI.
# Bieżąca wersja pliku ma inline `gitleaks:allow`, ale skan PR-owy chodzi po
# COMMITACH gałęzi, więc historyczny f2232b2 (przed dopiskiem) trzeba wyciszyć
# odciskiem palca. Wpis jest przypięty do JEDNEGO commita, pliku, reguły i
# numeru linii — nie rozluźnia skanera nigdzie indziej.
f2232b25b401c756daf94f6f37847bfc4708b085:src/lib/curriculum/__tests__/placement-service.integration.test.ts:generic-api-key:31

# Skutek uboczny naprawy wyżej: commit 5f87b00 dopisał TEN plik z komentarzem
# cytującym literał, więc skan gałęzi znalazł go w .gitleaksignore. Treść jest
# już poprawiona (cf37c61), ale commit 5f87b00 zostaje w zakresie skanu PR-a.
# Oba wpisy 1E.7 to RUSZTOWANIE SKANU GAŁĘZI, nie realne wyciszenia: po
# squash-merge main dostaje jeden commit z czystą treścią (inline
# `gitleaks:allow` w teście, zero literału tutaj), więc nocny pełny skan main
# jest zielony bez nich. Wolno je przyciąć przy najbliższym sprzątaniu.
5f87b00a264fc1e0bd1f7d91f2917f5dbd7357f6:.gitleaksignore:generic-api-key:7

# #344 (W21) — atrapy poświadczeń w strażniku kontraktu odmowy logowania.
# Trzy trafienia `generic-api-key` na polach o nazwie poświadczenia w atrapach
# żądań. To NIE są sekrety: każdy z tych przypadków dostaje odmowę, zanim
# cokolwiek sprawdzi to pole (bramka listy dostępu w `hooks.before`, a w drugim
# przypadku wyłączone logowanie mailem). Reguła łapie KSZTAŁT, nie znaczenie.
#
# PIERWSZEŃSTWO MIAŁA NAPRAWA, NIE WYCISZENIE — i została wykonana: bieżąca
# wersja pliku nie ma już żadnego napisu w tych miejscach, tylko nazwaną stałą
# (`cf. ATRAPA_NIEISTOTNA`), więc każdy KOLEJNY commit jest czysty bez tych
# wpisów. Wyciszenie jest konieczne wyłącznie dlatego, że skan zgłoszenia chodzi
# po COMMITACH gałęzi, a commit 5f503cf powstał przed naprawą — a historii nie
# przepisuję, bo trzej recenzenci przypięli swoje werdykty do konkretnych
# odcisków (5f503cf, e484914, 754965b) i przepisanie unieważniłoby ślad audytu.
#
# ⚠ NIE cytuj tu wartości — komentarz z literałem sam odpala regułę na TYM
# pliku (kosztowało już jeden przebieg przy 1E.7, wpis wyżej).
#
# Każdy wpis przypięty do JEDNEGO commita, pliku, reguły i numeru linii —
# nie rozluźnia skanera nigdzie indziej. Po scaleniu przez squash main dostaje
# jeden commit z czystą treścią, więc nocny pełny skan jest zielony bez nich;
# wolno je przyciąć przy najbliższym sprzątaniu (jak dwa wpisy 1E.7 wyżej).
5f503cfa779bd26b34061686b1cfc2c6a81f0e61:src/lib/auth/__tests__/ksztalt-odmowy-biblioteki.test.ts:generic-api-key:56
5f503cfa779bd26b34061686b1cfc2c6a81f0e61:src/lib/auth/__tests__/ksztalt-odmowy-biblioteki.test.ts:generic-api-key:79
5f503cfa779bd26b34061686b1cfc2c6a81f0e61:src/lib/auth/__tests__/ksztalt-odmowy-biblioteki.test.ts:generic-api-key:99
